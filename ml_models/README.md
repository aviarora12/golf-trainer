# ML Models

MediaPipe Pose models are downloaded automatically at runtime by the
`mediapipe` package the first time `PoseDetector` is instantiated, so no
weights need to be committed here.

This directory is reserved for any custom or fine-tuned models added later
(e.g. swing-phase classifiers or club-detection models).
